<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset License - Envauth</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center p-8">
    <div class="bg-white rounded-lg shadow-lg p-8 max-w-md w-full">
        <h1 class="text-3xl font-bold text-gray-900 mb-2">Reset License</h1>
        <p class="text-gray-600 mb-6">Deactivate your license to activate it on a different machine or IP address</p>

        <?php if (isset($user) && !empty($user['username'])): ?>
            <div class="bg-gray-50 p-4 rounded-lg mb-6 text-sm">
                Logged in as: <strong class="text-gray-900"><?= htmlspecialchars($user['username']) ?></strong>
            </div>
        <?php endif; ?>

        <?php if ($this->flash()->get('success')): ?>
            <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg mb-6">
                <?= htmlspecialchars($this->flash()->get('success')) ?>
            </div>
            <p class="text-gray-600 mb-4">
                You can now activate this license on a new machine or IP address.
            </p>
            <a href="/license/reset" class="block w-full text-center bg-gray-600 hover:bg-gray-700 text-white font-medium py-3 px-4 rounded-lg transition-colors">
                Reset Another License
            </a>
        <?php elseif ($this->flash()->get('error')): ?>
            <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg mb-6">
                <?= htmlspecialchars($this->flash()->get('error')) ?>
            </div>
        <?php endif; ?>

        <?php if ($show_form ?? true): ?>
            <form method="POST" action="/license/reset" class="space-y-6">
                    <div>
                        <label for="purchase_code" class="block text-sm font-medium text-gray-700 mb-2">
                            Envato Purchase Code *
                        </label>
                        <input
                            type="text"
                            id="purchase_code"
                            name="purchase_code"
                            required
                            placeholder="Enter your Envato purchase code"
                            value="<?= htmlspecialchars($purchase_code ?? '') ?>"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 outline-none transition"
                        >
                    </div>

                <div>
                    <label for="reason" class="block text-sm font-medium text-gray-700 mb-2">
                        Reason (Optional)
                    </label>
                    <textarea
                        id="reason"
                        name="reason"
                        rows="4"
                        placeholder="Why are you resetting this license? (e.g., Moving to a new server, hardware change, etc.)"
                        class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 outline-none transition resize-y"
                    ><?= htmlspecialchars($reason ?? '') ?></textarea>
                </div>

                <button
                    type="submit"
                    class="w-full bg-green-600 hover:bg-green-700 text-white font-medium py-3 px-4 rounded-lg transition-colors"
                >
                    Reset License
                </button>
            </form>
        <?php endif; ?>

        <div class="mt-8 text-center">
            <a href="/oauth/logout" class="text-gray-600 hover:text-gray-900 text-sm">Logout</a>
        </div>
    </div>
</body>
</html>
